version: "3.2"

services:
  applepaymerchant:
    image: clearhaus/applepaymerchant
    build:
      context: ./
      dockerfile: ./build/Dockerfile
    stdin_open: true
    tty: true
    ports:
      - "8080:8080"
    volumes:
      - ./:/opt/applepaymerchant
